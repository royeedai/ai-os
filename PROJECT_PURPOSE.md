# AI-OS 项目目的

## 1. 最终目的

AI-OS 的最终目的不是让 AI 更自动写代码，而是让 AI 更稳定地把项目做对：

- 先把目标和成功标准说清
- 先锁关键设计和关键逻辑
- 再推进实现、验证和交付
- 全程保留可恢复的项目记忆和证据

它服务于已经开始用 AI 参与项目开发，但不满足于"代码生成很快、结果却不稳"的个人开发者和团队。

AI-OS 想解决的问题不是"少一个 prompt"，而是"如何让 AI 在多个 session、多个阶段里持续把项目带到高质量结果"。

## 2. 2026 年的定位（为什么 AI-OS 现在比以前更重要）

2026 年的 AI 编程生态已经跨过几个临界点：

- **模型自验证**：Opus 4.7 SWE-bench Verified 87.6%，模型在报告完成前会自己先验证一次
- **指令字面化**：frontier 模型严格按 prompt 执行，模糊的需求会被等比例放大成错误
- **长时域自主**：Cursor 52 小时、Codex 25 小时、Kimi K2.6 300 sub-agents 已是常态
- **原生记忆**：Claude Code `MEMORY.md`、Cursor notepads、Kiro steering 都在内卷 agent 记忆层
- **SDD 工具**：Kiro、spec-kit 覆盖 0→1 立项，但不覆盖变更、调试、恢复、复盘

**模型越强、跑得越久，"把项目做对"的瓶颈就越从"写代码"迁移到"定义正确目标、锁定正确设计、提供证据化闭环、保留可恢复的记忆"**。这正是 AI-OS 的主战场。

AI-OS 的反向边界同样清晰：

- 不帮你跑 52 小时（那是 harness 的事）
- 不帮你生成代码（那是模型的事）
- 不做自动记忆抽取（原生工具已经做了）
- 不做 agent orchestration（Cursor / Codex 已经做了）

AI-OS 的独占赛道：**跨 agent、全生命周期、极简的 AI 交付宪法和工件规范**。

## 3. 五条核心要求

### 3.1 目标与用户确认优先

所有流程先服务用户真实目标、成功标准和确认节奏，而不是服务既有模板、工具习惯或 AI 自己的默认偏好。

### 3.2 关键设计与逻辑先锁定

关键页面、信息架构、关键交互、核心接口、状态流转和关键异常路径未确认前，不进入大规模实现。长时域自主运行下，这一条比以往任何时候都重要——模糊的设计会被放大成几万行错误代码。

### 3.3 自适应治理

工件和流程深度由需求清晰度、项目风险、项目类型和用户质量要求共同决定，不再固定按项目类别套一条重流程。

### 3.4 证据化完成

"完成"必须同时包含：

- 产品效果证据
- 逻辑正确性证据
- 工程可交付证据
- 代码状态 / 数据状态 / 运行状态三段拆分

模型内建的 self-verification 不等同于项目级证据化完成，二者是互补关系。

### 3.5 可恢复的项目记忆

AI 必须能跨会话恢复项目目标、当前阶段、已确认设计、待确认问题、最近偏差和下一步，不是只靠聊天上下文。与 Claude Code `MEMORY.md`、Cursor notepads 等工具原生记忆的职责分工：AI-OS 负责稳定决策和跨 session 恢复协议，原生工具负责会话级记忆。

## 4. 产品形态

AI-OS 的产品不是一堆 CLI 命令，是**一份可跨 agent 通用的 AI 交付宪法 + 最小工件集 + 参考实现**。

- 规范：`AGENTS.md`（≤150 行）+ 12 组工件 schema（`docs/artifacts.md`）
- 参考实现：零依赖 Node.js CLI，3 个命令（`install` / `doctor` / `upgrade`）
- 生态：可被 Kiro / spec-kit / Cursor / Claude Code 集成

## 5. 新需求筛选法

每个关于 AI-OS 本身的新需求，都先回答 4 个问题：

1. 它是不是在提升"把项目做对"的能力，而不是只让某个动作更自动？
2. 它提升的是目标确认、设计锁定、逻辑锁定、证据完成，还是可恢复记忆中的哪一项？
3. 它更适合落在 AGENTS.md 宪法、工件 schema、CLI 还是 `docs/` / `examples/`？
4. 它是在减少错误交付，还是只是在增加一层新概念？

允许的结论只有 4 类：

- 进入 AGENTS.md 宪法
- 进入工件 schema（12 组之一）
- 进入 CLI 或 `docs/`
- 明确不纳入

**默认答案是"不纳入"**。AI 生态每 2 个月变一次，AI-OS 的内核必须比生态更稳。

## 6. 当前阶段重点（v9）

现阶段 AI-OS 优先推进：

- 用 shared-root + `lanes/default/` 固定 canonical layout
- 让 install / doctor / upgrade / docs / examples / tests 只表达一套默认真相
- 保持内核表面积最小，同时提升多人协作和跨 session 恢复的一致性
- 明确根层共享工件与当前 lane 工件的职责边界
- 保持 12 组工件的完整能力，不牺牲 brownfield / high-risk / reverse-spec 的可靠性

更多见 `docs/migrate-to-v9.md` 和 `docs/constitution-spec.md`。
