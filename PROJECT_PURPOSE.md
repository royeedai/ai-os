# AI-OS 项目目的

## 1. 最终目的

AI-OS 的最终目的，不是让 AI 更自动写代码，而是让 AI 更稳定地引导用户把项目做对：

- 先把目标和成功标准说清
- 先锁关键设计和关键逻辑
- 再推进实现、验证和交付
- 全程保留可恢复的项目记忆和证据

它服务的是已经开始用 AI 参与项目开发，但不满足于“代码生成很快、结果却不稳”的个人开发者和小团队。

AI-OS 想解决的问题，不是“少一个 prompt”，而是“如何让 AI 在多个 session、多个阶段里持续把项目带到高质量结果”。

## 2. 五条核心要求

### 2.1 目标与用户确认优先

所有流程先服务用户真实目标、成功标准和确认节奏，而不是服务既有模板、工具习惯或 AI 自己的默认偏好。

### 2.2 关键设计与逻辑先锁定

关键页面、信息架构、关键交互、核心接口、状态流转和关键异常路径未确认前，不进入大规模实现。

### 2.3 自适应治理

工件和流程深度由需求清晰度、项目风险、项目类型和用户质量要求共同决定，不再固定按项目类别套一条重流程。

### 2.4 证据化完成

“完成”必须同时包含：

- 产品效果证据
- 逻辑正确性证据
- 工程可交付证据

### 2.5 可恢复的项目记忆

AI 必须能跨会话恢复项目目标、当前阶段、已确认设计、待确认问题、最近偏差和下一步，而不是只靠聊天上下文。

## 3. 新需求筛选法

每个关于 AI-OS 本身的新需求，都先回答这 4 个问题：

1. 它是不是在提升“把项目做对”的能力，而不是只让某个动作更自动？
2. 它提升的是目标确认、设计锁定、逻辑锁定、证据完成，还是可恢复记忆中的哪一项？
3. 它更适合落在根层原则、workflow、skill、模板、CLI 还是示例 / 文档？
4. 它是在减少错误交付，还是只是在增加一层新概念？

允许的结论只有 4 类：

- 进入 `framework/`
- 留在 CLI / 示例 / 文档
- 留在项目工件
- 明确不纳入

## 4. 派生规则原则

根层只保留五条核心要求。更具体的规则，一律视为派生规则，落在：

- `framework/AGENTS.md`
- workflow
- skill
- 模板
- CLI 校验
- evals / examples

常见派生规则示例：

| 派生规则 | 归属核心要求 |
| --- | --- |
| 先锁关键页面再完整开发 | 关键设计与逻辑先锁定 |
| reverse-spec 要有 parity 证据 | 证据化完成 |
| 高风险任务必须有审批点 | 证据化完成 |
| 局部改动不默认全仓扫描 | 目标与用户确认优先 |
| 新 session 先看 `STATE.md` | 可恢复的项目记忆 |

## 5. 当前阶段重点

现阶段 AI-OS 优先推进这些方向：

- 让 AI 更擅长引导用户逐步确认，而不是直接给出一版“看起来差不多”的实现
- 让关键设计和关键逻辑的确认更早发生
- 让质量门禁能同时拦住“效果像但逻辑错”和“逻辑对但产品差”两类伪完成
- 让项目在中断后能以更低成本恢复
