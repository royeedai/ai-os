# [当前交付主题] Design

> 未经用户确认的核心设计决策，不得进入完整实现。如果需求边界变化，先回到 lane 的 `MISSION.md` 和 `baseline-log/CR-*.md`。

## 1. 设计目标

- **本轮设计目标**：
- **需要先锁定的关键页面 / 交互 / 接口**：
- **必须用户确认的核心设计决策**：

## 2. 信息架构（UI 项目必填）

- **入口与导航骨架**：
- **一级 / 二级结构**：
- **关键信息优先级**：

## 3. 关键页面与交互（UI 项目必填）

| 页面 / 入口 | 目标 | 关键元素 | 关键操作 | 是否核心决策 | 确认状态 |
|---|---|---|---|---|---|
| [页面名] | [目标] | [关键元素] | [关键操作] | yes / no | pending |

## 4. 核心接口与数据模型（API 项目必填）

> 本节是契约层（数据模型 / 接口 / 状态机）。契约必须先于实现锁定；确认状态为 pending 的契约不得进入大规模实现。

| 接口 / 模型 | 用途 | 关键字段 | 状态流转 | 是否核心决策 | 确认状态 |
|---|---|---|---|---|---|
| [接口名] | [用途] | [字段列表] | [状态流转] | yes / no | pending |

## 5. 关键流程

1. [用户操作或系统触发]
2. [系统响应]
3. [关键分支 / 异常路径]
4. [边界条件 / 失败恢复]

## 6. 共享基础设施审计（brownfield / change / reverse-spec 必填）

- **受影响的共享组件**：
- **受影响的接口 / 页面清单**：
- **同仓正常实现对照**：
- **副作用清单**：

## 7. 对照参考（reverse-spec 必填）

- **原始参考清单**：
- **字段级 / 行为级对照摘要**：
- **仍待解决差异**：

## 8. 验收标准

| AC ID | 需求 ID | 验收描述 | 验证方式 | 证据 |
|---|---|---|---|---|
| AC-001 | REQ-001 | [验收描述] | 自动化 / 手动 / 运行时观察 | [证据文件] |

## 9. 反述确认门（设计锁定前必经）

> 进入大规模实现前，agent 先反述对关键设计的理解，用户确认或校正后才锁定（见 `AGENTS.md` §1 反述确认）。

- **agent 反述的关键设计理解**：[目标 / 信息架构 / 核心接口契约 / 状态流转 / 关键异常路径]
- **用户确认 / 校正**：[确认了什么、由谁确认、需校正什么、影响哪些模块]
- **确认日期**：
