---
trigger_source: manual
first_baseline_id: ""
risk_source: delivery-governance
failure_mode: example-failure-mode
harm: delivery-regression
artifact_gate: constitution-gate
---

# Eval Example

> 升格到 `evals/` 的失败模式样例必须带上方 frontmatter（`trigger_source`: `manual` / `promoted-from-verification-matrix`；`first_baseline_id` 指向首次出现的 baseline-log ID）。

- **Failure mode**：
- **Trigger**：
- **Expected**：
- **Observed**：
- **Guard update**：
