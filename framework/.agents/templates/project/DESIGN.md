# [项目名] Design

> 未经用户确认的核心设计决策，不得进入完整实现。如果需求边界变化，先回到 `MISSION.md` 和 `baseline-log/CR-*.md`。
> 本模板所有内容必须严格基于用户确认的需求基准生成，禁止擅自超出范围、脑补内容。

## 1. 设计目标

- **本轮设计目标**：
- **需要先锁定的关键页面 / 交互 / 接口**：
- **必须用户确认的核心设计决策**：
- **允许 AI 自行处理的非核心细节**：
- **允许后补的细节**：

## 2. 信息架构（UI 项目必填）

- **入口与导航骨架**：
- **一级 / 二级结构**：
- **关键上下文切换点**：
- **关键信息优先级**：

## 3. 关键页面与交互（UI 项目必填）

| 页面 / 入口 | 目标 | 关键元素 | 关键操作 | 是否核心决策 | 确认状态 |
|-------------|------|----------|----------|--------------|----------|
| [页面名] | [目标] | [表格/表单/看板等] | [新增/编辑/状态流转等] | yes / no | pending |

## 4. 核心接口与数据模型（API 项目必填）

| 接口 / 模型 | 用途 | 关键字段 | 状态流转 | 是否核心决策 | 确认状态 |
|---|---|---|---|---|---|
| [接口名] | [用途] | [字段列表] | [状态转移] | yes / no | pending |

## 5. 关键流程

1. [用户操作或系统触发]
2. [系统响应]
3. [关键分支 / 异常路径]
4. [边界条件 / 失败恢复]

## 6. 视觉方向（UI 项目必填）

- **视觉参考**：
- **信息密度 / 布局原则**：
- **组件与交互风格**：
- **明确不采用的风格**：
- **与既有设计系统或参考对象的差异**：

## 7. 共享层副作用清单（brownfield / change / reverse-spec 必填）

> 触及 shared layer、全局包装层、通用抽象或跨切面基础设施时，必须先列出影响清单，否则不进入实现。

- **受影响的共享组件**（request wrapper / interceptor / DTO / adapter / middleware / router guard / BaseEntity）：
- **受影响的接口 / 页面清单**：
- **无字段 / 无上下文 / 无鉴权场景**：
- **白名单 / 排除清单需求**：
- **同仓正常实现对照**：

## 8. 对照参考（reverse-spec 必填）

- **原始参考清单**（截图 / API / 源码）：
- **字段级 / 行为级对照摘要**：
- **相对参考对象的明确差异**：
- **仍待解决差异**：

## 9. 方案选型依据

- **为什么选择当前方案**：
- **备选方案与放弃原因**：
- **与需求基准的对应关系**：

## 10. 核心约束

- **必须遵守的技术 / 产品约束**：
- **路由 / 入口契约对照**：
- **Schema / 存储一致性说明**：
- **不可越过的范围边界**：
- **依赖前提**：

## 11. 设计确认记录

- [日期]：[确认了什么、由谁确认、影响哪些页面 / 模块]
- [日期]：[暂缓确认什么、为什么暂缓、下一步如何补齐]

## 12. 待确认项

- **仍待确认的设计项**：
- **执行边界与非目标**：
- **触发升级到 change-request 或重新设计的条件**：

## 13. 验收标准

> 验收标准必须在实现前锁定，并与需求点一一对应。每一条验收都应可机器校验或由用户直接观察到。

### 质量档位

- **当前交付档位**：exploratory / standard / high-risk
- **档位对应的专项审查**：

### 验收门

| 门 | 通过条件 | 证据来源 |
|---|---|---|
| 设计确认门 | 用户明确确认关键设计决策 | DESIGN §11 |
| 逻辑确认门 | 业务规则、状态流转、关键异常路径在 spec / DESIGN 中锁定且被用户确认 | specs/ + DESIGN |
| 实现质量门 | 原生静态校验通过 + 测试套件无回归 + 证据归档 | build log / test log |
| 交付质量门 | 双清单完备（已实现 / 未纳入 / 验证 / 回滚 / AI vs 人工）| release-plan.md |
| 对照一致性门（reverse-spec）| design-pack/parity-map.md 对照项全部有结论 | parity-map.md |

### 逐项验收映射

| AC ID | 需求 ID | 验收描述 | 验证方式 | 证据 |
|---|---|---|---|---|
| AC-001 | REQ-001 | [验收描述] | 自动化测试 / 手动 / 运行时观察 | [证据文件] |

### 异常路径与回归

- **必须验证的异常路径**：
- **必须验证的空数据 / 权限拒绝 / 超时 / 部分失败场景**：
- **必须对照的回归基线**：

### 静态校验证据要求

- **项目原生静态校验命令**（compile / type-check / build）：
- **IDE 内置诊断**（ReadLints 等）：**只能作为辅助，不得作为唯一校验证据**

## 14. 风险与注意事项

- **主要风险**：
- **注意事项**：
- **触发升级到 change-request 的条件**：
