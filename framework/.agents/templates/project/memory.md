# 项目记忆

> 只记录稳定事实：已确认决策、长期约束、用户偏好、已知坑点、项目级约定、跨层契约。临时需求或未确认想法不要写入这里，应先留在 `STATE.md`、`baseline-log/` 或相关 spec 草案。
> 本模板所有内容必须严格基于用户确认的需求基准生成，禁止擅自超出范围、脑补内容。
> 所有条目 ID 必须在整份文件内全局唯一；归档时从 `## active` 移动到 `## archived`，不复制一份同 ID 内容，也不删除。

## 元数据

- **最后更新**：
- **活跃条目数**：
- **归档条目数**：

---

## active

### 1. 设计决策

#### DD-001: [决策标题]

- **决策**：
- **原因**：
- **影响范围**：
- **确认来源**：
- **日期**：

### 2. 逻辑与契约决策

#### CD-001: [决策标题]

- **决策**：
- **原因**：
- **影响范围**：
- **确认来源**：
- **日期**：

### 3. 工程约束

#### EC-001: [约束标题]

- **约束**：
- **原因**：
- **影响范围**：
- **确认来源**：
- **日期**：

### 4. 用户偏好

#### PF-001: [偏好标题]

- **偏好**：
- **适用范围**：
- **来源**：
- **日期**：

### 5. 已知坑点

#### PT-001: [坑点标题]

- **问题**：
- **根因**：
- **绕行方案**：
- **影响范围**：
- **日期**：

### 6. 技术债追踪

> 记录已知技术债。技术债消除后归档而非删除。

#### TD-001: [债务标题]

- **类型**：pattern-drift / missing-test / architecture-violation / deprecated-dependency
- **严重度**：high / medium / low
- **来源里程碑**：
- **影响范围**：
- **消除计划**：
- **日期**：

### 7. 项目通用约定

> 项目级编码约定，防止跨 session / 多人协作时实现风格漂移。

#### 命名约定

- **文件命名**：
- **目录结构**：
- **变量 / 函数命名**：
- **类型命名**：

#### 代码模式

- **错误处理模式**：
- **日志模式**：
- **异步模式**：
- **状态管理模式**：

#### 分层与依赖

- **分层边界**：
- **禁止的跨层依赖**：
- **公共工具放置位置**：

### 8. 跨层契约登记表

> 记录跨层隐式契约，防止单边改动导致契约擦除。每类契约必须登记真理源位置、所有参与方、变更影响清单。

#### 8.1 HTTP 状态码 ↔ 业务码 ↔ 客户端行为映射

| HTTP 状态 | 业务码 | 客户端行为 | 定义位置 | 消费方 |
|---|---|---|---|---|
| 200 | success | 正常渲染 | backend/response.ts | frontend/api-client.ts |

#### 8.2 Wire 类型契约（接口数据类型）

| 字段 | 类型 | 精度 / 范围 | 产出方 | 消费方 | 备注 |
|---|---|---|---|---|---|
| user.id | string | uuid v4 | backend/user.service.ts | frontend/user-store.ts | 禁止使用 number |

#### 8.3 名单型常量（enum / 枚举）反向真理源

| 常量名 | 真理源位置 | 消费方 | 同步策略 |
|---|---|---|---|
| OrderStatus | backend/enums.ts | frontend、mobile | 一处变更必须跟改所有消费方 |

#### 8.4 敏感数据 service 方法语义档位

| service 方法 | 脱敏档位 | 禁止使用场景 | 禁止绕过方式 |
|---|---|---|---|
| UserService.findById | 完整 | 列表展示 | 禁止直连 Repository |

#### 8.5 中间件 / 查询引擎方言契约

| 场景 | 方言 / 约定 | 影响面 | 禁止 |
|---|---|---|---|
| ORM 自动填充 | BaseEntity.createdBy | 所有继承实体 | 禁止手动赋值 |

---

## archived

> 已被取代、已过期或不再影响当前开发的条目移到这里。保留原文以备回溯。归档条目的 ID 保持不变，并在条目末追加归档原因和归档日期。
