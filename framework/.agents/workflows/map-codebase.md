---
name: map-codebase
description: 分析已有代码库的技术栈、架构、约定和模式（Brownfield 项目入口）
---

# 代码库分析流程

当在已有代码库上开始新项目或新模块时触发。适用于用户说"分析一下现有代码"、"我已经有个项目想加功能"、"先了解下代码库"等场景。

## 何时使用

- 第一次接手一个已有代码库
- 在 brownfield 项目上准备走 `/new-project` 或 `/new-module`
- 项目经历大重构后，需要重新建立统一认知

## 不该使用

- 只是 1-3 个文件的小改动
- 你已经熟悉代码库，且这次任务不依赖新增架构分析
- 当前需求只是上线检查或事故处理

## 会生成或更新什么

- `.ai-os/codebase-map.md`：技术栈、架构、约定、现有模式、资产盘点
- `.ai-os/verification-matrix.yaml`：路径到验证动作、重启命令、冷启动 Smoke 的基线
- `.ai-os/memory.md`：沉淀稳定约束与编码约定
- 后续 spec / tasks：必须引用 `.ai-os/codebase-map.md`

## 步骤

1. **技术栈识别**：扫描项目结构，识别语言、框架、包管理器、构建工具、测试框架
2. **架构分析**：分析代码组织模式（分层结构、模块边界、目录职责）
3. **约定提取**：识别命名约定（变量、文件、API 路径、数据库表）
4. **模式识别**：提取现有模式：
   - 错误处理方式（统一错误码 / HTTP 状态码 / 自定义异常）
   - 鉴权方式（JWT / Session / OAuth）
   - 数据访问模式（ORM / Repository / Raw SQL）
   - API 风格（REST / GraphQL / RPC）
   - 状态管理（前端）
   - 环境配置方式
5. **运行拓扑识别**：梳理现有服务 / 进程、build/start/restart 命令、冷启动 Smoke Check 入口、共享代码影响面
6. **现有资产盘点**：识别已有的数据库 schema、API 端点、页面路由、共享组件
7. **生成 `.ai-os/codebase-map.md`**：

```markdown
# 代码库分析报告

## 技术栈
- **语言**：
- **框架**：
- **包管理器**：
- **构建工具**：
- **测试框架**：
- **数据库**：

## 运行拓扑与验证基线

| 组件 | 启动命令 | 重启命令 | 冷启动 Smoke | 关键路径 |
|------|----------|----------|--------------|----------|

## 目录结构与职责

| 目录 | 职责 | 关键文件 |
|------|------|---------|

## 命名约定

| 维度 | 约定 | 示例 |
|------|------|------|
| 文件名 | | |
| 变量名 | | |
| API 路径 | | |
| 数据库表名 | | |

## 现有模式

| 模式 | 当前实现 | 关键代码位置 |
|------|---------|-------------|
| 错误处理 | | |
| 鉴权 | | |
| 数据访问 | | |
| API 风格 | | |

## 现有资产

### 数据库表
| 表名 | 用途 | 关键字段 |
|------|------|---------|

### API 端点
| 方法 | 路径 | 用途 |
|------|------|------|

### 页面路由（若有前端）
| 路径 | 页面 | 组件 |
|------|------|------|

## 需要遵守的约束
- [基于分析得出的硬性约束]
```

8. **更新验证矩阵**：把路径到验证动作的稳定映射写入 `.ai-os/verification-matrix.yaml`，作为后续 `affected`、任务拆解、发布计划的单一真相源
9. **写入 memory**：将分析结果中的稳定约束写入 `.ai-os/memory.md` 的"硬性约束"和"编码约定"部分
10. **后续引用**：后续 spec 编写和编码实现**必须参考** `.ai-os/codebase-map.md` 与 `.ai-os/verification-matrix.yaml`，确保新代码与现有风格一致

