---
name: init
description: 兼容别名，映射到 brownfield 模式的 /align
---

# /init（兼容别名）

请改用 `/align`，并把模式设为 `brownfield`。

## 目标

先把已有仓库的目标、当前阶段、主要模块、质量标准和待确认项写进 `MISSION.md` 与 `STATE.md`，再决定是否需要继续 `/design` 或 `/plan`。
